<div class="container">

  <h1>All restaurants</h1>

  <ul>
    <% @restaurants.each do |resto|%>
      <li>
        <%= link_to resto.name, restaurant_path(resto) %>
      </li>
    <% end %>
  </ul>

</div>
